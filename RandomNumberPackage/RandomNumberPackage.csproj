<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <PackageId>codemixt.RandomNumberHelper</PackageId>
    <Title>Random Number Helper</Title>
    <Authors>codemixt</Authors>
    <Description>get random number by length , max length is 9 so if you type length greater than 9 you will get number of 9 digits only , you can user package to generate phone active code , id etc.</Description>
  </PropertyGroup>

</Project>
